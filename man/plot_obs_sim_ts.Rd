% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-hydrograph.R
\name{plot_obs_sim_ts}
\alias{plot_obs_sim_ts}
\title{Plot observed vs. simulated time series}
\usage{
plot_obs_sim_ts(
  dt,
  x = "timestamp",
  obs = "observed",
  sim = "simulated",
  site_col = NULL,
  title = NULL
)
}
\arguments{
\item{dt}{A data.table with columns for timestamp, observed value, and
simulated value. Wide format: one row per timestamp (per site).}

\item{x}{Name of the timestamp column. Default \code{"timestamp"}.}

\item{obs}{Name of the observed value column. Default \code{"observed"}.}

\item{sim}{Name of the simulated value column. Default \code{"simulated"}.}

\item{site_col}{Optional column name for faceting by site.}

\item{title}{Plot title. Default \code{NULL}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a paired line chart with observed values in dark navy and simulated
values in teal. Optionally faceted by site.
}
\examples{
library(data.table)
dt <- make_obs_sim_dt()
plot_obs_sim_ts(dt, title = "Hindcast: Eden at Sheepmount")

}
