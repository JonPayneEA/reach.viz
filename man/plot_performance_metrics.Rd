% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-performance.R
\name{plot_performance_metrics}
\alias{plot_performance_metrics}
\title{Plot model performance metrics as a lollipop chart}
\usage{
plot_performance_metrics(
  dt,
  metric_col = "metric",
  value_col = "value",
  site_col = "site_id",
  thresholds = NULL
)
}
\arguments{
\item{dt}{A data.table in long format with columns for metric name,
value, and site identifier.}

\item{metric_col}{Name of the metric column. Default \code{"metric"}.}

\item{value_col}{Name of the numeric value column. Default \code{"value"}.}

\item{site_col}{Name of the site identifier column. Default \code{"site_id"}.}

\item{thresholds}{Optional named numeric vector of pass/fail thresholds,
one per metric name (e.g. \code{c(NSE = 0.6, KGE = 0.6)}).}
}
\value{
A \code{ggplot} object (faceted by metric).
}
\description{
Draws a lollipop (point-and-segment) chart of one or more performance metrics
across sites. Optional threshold lines mark the pass/fail boundary for each
metric. Faceted by metric.
}
\examples{
library(data.table)
dt <- make_performance_dt()
plot_performance_metrics(dt, thresholds = c(NSE = 0.6, KGE = 0.6))

}
