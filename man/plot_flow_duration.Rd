% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-hydrograph.R
\name{plot_flow_duration}
\alias{plot_flow_duration}
\title{Plot a flow duration curve}
\usage{
plot_flow_duration(
  dt,
  value_col = "qc_value",
  flag_col = "qc_flag",
  group_col = NULL,
  log_y = TRUE
)
}
\arguments{
\item{dt}{A data.table with at minimum \code{value_col} and \code{flag_col}.}

\item{value_col}{Name of the flow/value column. Default \code{"qc_value"}.}

\item{flag_col}{Name of the QC flag column (flags 4–5 excluded).
Default \code{"qc_flag"}.}

\item{group_col}{Optional grouping column name (e.g. \code{"site_id"}).}

\item{log_y}{Log-transform the Y axis. Default \code{TRUE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Computes exceedance probabilities from the supplied time series and draws a
flow duration curve. QC flags 4 and 5 are excluded before ranking. An
optional grouping variable (e.g. site, season) produces overlaid curves.
}
\examples{
library(data.table)
dt <- make_hydro_dt()
plot_flow_duration(dt)
plot_flow_duration(dt, group_col = "site_id", log_y = TRUE)

}
