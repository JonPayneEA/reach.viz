% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-ensemble.R
\name{plot_ensemble_ribbon}
\alias{plot_ensemble_ribbon}
\title{Plot ensemble forecast as percentile ribbons}
\usage{
plot_ensemble_ribbon(
  dt,
  x = "timestamp",
  value_col = "value",
  member_col = "ensemble_member",
  probs = c(0.1, 0.25, 0.5, 0.75, 0.9),
  obs_dt = NULL,
  site_col = NULL
)
}
\arguments{
\item{dt}{A data.table in long ensemble format with columns for
timestamp, value, and ensemble member.}

\item{x}{Name of the timestamp column. Default \code{"timestamp"}.}

\item{value_col}{Name of the value column. Default \code{"value"}.}

\item{member_col}{Name of the ensemble member identifier column.
Default \code{"ensemble_member"}.}

\item{probs}{Numeric vector of probability levels. Two pairs of
symmetric quantiles plus the median are expected: the outer pair forms the
widest ribbon, the inner pair the narrower band, and the central value the
median line. Default \code{c(0.1, 0.25, 0.5, 0.75, 0.9)}.}

\item{obs_dt}{Optional data.table of observed values. Must have the same
\code{x} column and a column named \code{"value"}. Plotted as a dark navy line.}

\item{site_col}{Optional column for faceting by site.}
}
\value{
A \code{ggplot} object.
}
\description{
Computes quantiles across ensemble members at each timestep and renders
them as nested ribbons. The median is drawn as a solid line. An optional
observed time series can be overlaid.
}
\examples{
library(data.table)
ens <- make_ensemble_dt()
plot_ensemble_ribbon(ens)

}
