% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-condition.R
\name{plot_condition_register}
\alias{plot_condition_register}
\title{Plot the model condition register overview}
\usage{
plot_condition_register(
  dt,
  model_col = "model_id",
  score_col = "condition_score",
  rating_col = "condition_rating",
  tier_col = "tier",
  status_col = "status"
)
}
\arguments{
\item{dt}{A data.table with one row per model. Required columns:
\code{model_id}, \code{condition_score} (integer 0–10), \code{condition_rating}
(character), \code{tier} (character), and \code{status} (character).}

\item{model_col}{Name of the model identifier column. Default \code{"model_id"}.}

\item{score_col}{Name of the condition score column.
Default \code{"condition_score"}.}

\item{rating_col}{Name of the condition rating column.
Default \code{"condition_rating"}.}

\item{tier_col}{Name of the tier column. Default \code{"tier"}.}

\item{status_col}{Name of the status column. Default \code{"status"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a horizontal lollipop chart of all models in the register, sorted by
condition score (worst at the top). Point colour encodes the condition rating;
point shape encodes tier; suspended or deprecated models are shown in grey.
}
\examples{
library(data.table)
dt <- make_register_dt()
plot_condition_register(dt)

}
