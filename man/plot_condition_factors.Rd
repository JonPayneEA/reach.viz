% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-condition.R
\name{plot_condition_factors}
\alias{plot_condition_factors}
\title{Plot model condition factor scores as a radar chart}
\usage{
plot_condition_factors(
  dt,
  model_col = "model_id",
  factor_col = "factor",
  score_col = "score"
)
}
\arguments{
\item{dt}{A data.table in long format with one row per factor per
model. Required columns: \code{model_id} (or the column named by \code{model_col}),
\code{factor} (character, one of the five standard factor codes or labels), and
\code{score} (integer 0–2).}

\item{model_col}{Name of the model identifier column. Default \code{"model_id"}.}

\item{factor_col}{Name of the factor column. Default \code{"factor"}.}

\item{score_col}{Name of the score column. Default \code{"score"}.}
}
\value{
A \code{ggplot} object using \code{coord_polar()}.
}
\description{
Renders a polar-coordinate radar chart showing the five condition assessment
factors for one or more models. Each factor is scored 0–2 (Poor, Adequate,
Good). When multiple models are supplied, they are faceted.
}
\details{
Factor codes are automatically matched to display labels. If factor values
in \code{dt} do not match the standard codes, they are used as-is.

The maximum polygon (score = 2 on all factors) is shown as a light grey
reference. The actual model polygon is filled with the colour corresponding
to the model's overall condition rating.
}
\examples{
library(data.table)
dt <- make_condition_dt()
plot_condition_factors(dt)
plot_condition_factors(dt[model_id == "FMP_EDEN_v3"])

}
