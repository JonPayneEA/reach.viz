% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-hydrograph.R
\name{plot_hydrograph}
\alias{plot_hydrograph}
\title{Plot a hydrometric time series with QC flag colour encoding}
\usage{
plot_hydrograph(
  dt,
  x = "timestamp",
  y = "qc_value",
  flag_col = "qc_flag",
  site_col = NULL,
  show_raw = FALSE,
  title = NULL,
  y_label = NULL
)
}
\arguments{
\item{dt}{A data.table in Silver or Gold format containing at minimum the
columns \code{timestamp} (POSIXct), \code{qc_value} (numeric), and \code{qc_flag}
(integer, 1–6).}

\item{x}{Name of the timestamp column. Default \code{"timestamp"}.}

\item{y}{Name of the analysis-ready value column. Default \code{"qc_value"}.}

\item{flag_col}{Name of the QC flag column. Default \code{"qc_flag"}.}

\item{site_col}{Optional column name for site identifier. When supplied the
plot is faceted by site using \code{ggplot2::facet_wrap()}.}

\item{show_raw}{Logical. When \code{TRUE} and \code{bronze_value} column is present,
a faint grey background line shows the raw Bronze value. Default \code{FALSE}.}

\item{title}{Plot title string. Default \code{NULL}.}

\item{y_label}{Y-axis label. When \code{NULL} (default) the label is derived
from the \code{data_type} column if present, otherwise \code{y} is used.}
}
\value{
A \code{ggplot} object. Add further layers with \code{+}.
}
\description{
Draws a line plot of hydrometric data where line colour encodes the QC flag
value. Flags 4 and 5 (Rejected, No data) are drawn as gaps. An optional
background layer shows the raw Bronze value for comparison.
}
\examples{
library(data.table)
dt <- make_hydro_dt()
plot_hydrograph(dt)
plot_hydrograph(dt, site_col = "site_id", title = "Hourly flow by site")

}
