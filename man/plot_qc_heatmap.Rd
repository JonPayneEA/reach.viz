% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-qc.R
\name{plot_qc_heatmap}
\alias{plot_qc_heatmap}
\title{Plot a QC flag distribution heatmap}
\usage{
plot_qc_heatmap(
  dt,
  time_col = "timestamp",
  flag_col = "qc_flag",
  site_col = "site_id",
  period = "month",
  flag_shown = 1:6
)
}
\arguments{
\item{dt}{A data.table with columns \code{timestamp}, \code{qc_flag}, and
\code{site_id}.}

\item{time_col}{Name of the timestamp column. Default \code{"timestamp"}.}

\item{flag_col}{Name of the QC flag column. Default \code{"qc_flag"}.}

\item{site_col}{Name of the site identifier column. Default \code{"site_id"}.}

\item{period}{Aggregation period: \code{"day"}, \code{"week"}, or \code{"month"}.
Default \code{"month"}.}

\item{flag_shown}{Integer vector of flag values to display. Defaults to
flags 1–6.}
}
\value{
A \code{ggplot} object (faceted by flag value, one tile per period × site).
}
\description{
Aggregates QC flags by time period and site, then renders a tile heatmap
showing flag proportions. Useful for a quick visual audit of a dataset's
quality across time and space.
}
\details{
Each tile shows the proportion of observations in that period × site that
carry the given flag. Tiles are filled from white (0\%) to the flag's
canonical colour (100\%).
}
\examples{
library(data.table)
dt <- make_hydro_dt()
plot_qc_heatmap(dt)
plot_qc_heatmap(dt, period = "week")

}
