% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-ensemble.R
\name{plot_ensemble_spaghetti}
\alias{plot_ensemble_spaghetti}
\title{Plot ensemble forecast as a spaghetti plot}
\usage{
plot_ensemble_spaghetti(
  dt,
  x = "timestamp",
  value_col = "value",
  member_col = "ensemble_member",
  highlight_median = TRUE,
  alpha = 0.25,
  obs_dt = NULL,
  site_col = NULL
)
}
\arguments{
\item{dt}{A data.table in long ensemble format.}

\item{x}{Name of the timestamp column. Default \code{"timestamp"}.}

\item{value_col}{Name of the value column. Default \code{"value"}.}

\item{member_col}{Name of the ensemble member column.
Default \code{"ensemble_member"}.}

\item{highlight_median}{Logical. Draw the ensemble median as a solid navy
line. Default \code{TRUE}.}

\item{alpha}{Opacity of individual member lines. Default \code{0.25}.}

\item{obs_dt}{Optional observed data.table (same schema as for
\code{\link[=plot_ensemble_ribbon]{plot_ensemble_ribbon()}}).}

\item{site_col}{Optional column for faceting by site.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws all individual ensemble member traces as thin lines with adjustable
opacity. Optionally highlights the ensemble median as a heavier line and
overlays an observed series.
}
\examples{
library(data.table)
ens <- make_ensemble_dt()
plot_ensemble_spaghetti(ens)

}
